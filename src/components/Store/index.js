import React from 'react';
import styles from './index.module.sass';

function Store(device) {
	return (
		<>
			<div className={`${styles.store} f1 w-100`}>Tixili</div>
			<div>dsfd</div>
		</>
	);
}

export default Store;
